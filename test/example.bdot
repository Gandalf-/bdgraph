#!/usr/local/bin/bdgraph
# a <- b,c,d == if a,b,c then d
# a -> b,c,d == if a then b,c,d

  1:  Find a fallen log
  2:  Find an ax
  3:  Chop up the log
  4:  Make kindling
  5:  Make a fire

  6: @Go to the store
  7:  Buy ingredients
  8:  Buy matches

  9:  Make smores!

options
  color_next
  cleanup
  color_complete

dependencies
  3  <- 1,2
  4  <- 3
  5  <- 4
  7  <- 6
  8  <- 6
  9  <- 5,7,8
